version: 2
jobs:
  build:
    working_directory: /dockerapp
    docker:
      - image: docker
    steps:
      - checkout
      - setup_remote_docker
      - run:
          name: test command
          command: |
            echo "test cicd"