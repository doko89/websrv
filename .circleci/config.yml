version: 2
jobs:
  build:
    working_directory: /websrv
    docker:
      image: docker
    steps:
      - checkout
      - setup_remote_docker

workflows:
  version: 2
  build_and_push:
    jobs:
      - build
